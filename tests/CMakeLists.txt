set(CMAKE_SKIP_BUILD_RPATH FALSE)
set(CMAKE_BUILD_WITH_INSTALL_RPATH FALSE)
set(CMAKE_INSTALL_RPATH "${CMAKE_INSTALL_PREFIX}/lib")
set(CMAKE_INSTALL_RPATH_USE_LINK_PATH TRUE)

include(CTest)
enable_testing()

function(NEW_TEST test_name)
   add_executable(${test_name} ${test_name}.cpp) 
   target_link_libraries(${test_name} fmanii)
   target_include_directories(${test_name} PRIVATE ${PROJECT_SOURCE_DIR})
   add_dependencies(${test_name} fmanii)
   add_test(NAME ${test_name} COMMAND ${test_name})
endfunction()

message(STATUS ${PROJECT_SOURCE_DIR})
   add_executable(TestParse TestParse.cpp)
   target_link_libraries(TestParse fmanii)
   target_include_directories(TestParse PRIVATE ${PROJECT_SOURCE_DIR})
   add_dependencies(TestParse fmanii)
   add_test(NAME TestParse COMMAND TestParse)